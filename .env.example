# Example environment variables
# For development, mise.toml provides VITE_* vars automatically via its [env] section.
# For production, set these in your deployment platform.

# Sentry Configuration (optional)
# ================================
VITE_SENTRY_DSN=

# Algolia Search Configuration
# ============================
# Get these values from your Algolia dashboard
VITE_ALGOLIA_APP_ID=your_app_id
VITE_ALGOLIA_SEARCH_KEY=your_search_only_key
VITE_ALGOLIA_INDEX_NAME=your_index_name

# Git Worktree Setup
# ==================
# For running multiple branches simultaneously, create mise.local.toml (gitignored)
# with different ports. URLs are computed automatically from these two values:
#
# mise.local.toml for worktree on different ports:
# ```toml
# [vars]
# port = "8081"
# api_port = "8001"
# ```
#
# This automatically sets:
# - VITE_PORT=8081
# - VITE_API_PORT=8001
# - VITE_API_ROOT_URL=http://localhost:8001
# - VITE_PUBLIC_URL=http://localhost:8081/public
